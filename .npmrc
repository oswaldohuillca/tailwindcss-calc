# Publish config
access=public

# Auth token is injected by CI (GitHub Actions) via the NODE_AUTH_TOKEN
# env var, populated from the NPM_TOKEN repository secret. npm only
# substitutes ${...} syntax, so this must use ${NODE_AUTH_TOKEN}.
//registry.npmjs.org/:_authToken=${NODE_AUTH_TOKEN}
